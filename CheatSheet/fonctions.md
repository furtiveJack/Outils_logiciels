# Les fonctions

Syntaxe générale :
```
  def function():
      return
```

On peut renvoyer des tuples :
```
  def tuples(a, b):
      return a, b
```

Condition ternaire :

```
    def min(a, b):
        return a if (a < b) else b
```